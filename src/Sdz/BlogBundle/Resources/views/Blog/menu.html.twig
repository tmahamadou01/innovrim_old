<ul>
	<h2>Activités </h2>
	<hr>

	<ul class="nav nav-pills nav-stacked-bis">
		{% for article in liste_articles %}
			<li>
				<a href="{{ path('sdz_blog_voir', {'id': article.id}) }}">{{ article.titre }}</a>
			</li>
		{% endfor %}
	</ul>

</ul>
